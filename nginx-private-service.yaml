apiVersion: v1
kind: Service
metadata:
  name: nginx-private-service
  namespace: default
  labels:
    app: nginx-private
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081
      protocol: TCP
      name: http
  selector:
    app: nginx-private
